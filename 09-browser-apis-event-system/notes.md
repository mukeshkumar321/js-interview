# 09 Browser APIs & Event System

## DOM & Browser Fundamentals

1. What are Browser APIs and how do they differ from JavaScript language features?
2. What is the DOM and how does the browser create it?
3. What is the difference between the DOM and the Virtual DOM?
4. How do you select DOM elements in JavaScript, and when would you use different DOM selection methods?

## Event System

5. What is Event Propagation?
6. What is the difference between Event Bubbling and Event Capturing?
7. What is Event Delegation and why is it useful?
8. How does Event Delegation improve performance?
9. What is the difference between `event.target` and `event.currentTarget`?
10. What is the difference between `preventDefault()` and `stopPropagation()`?
11. In what order are event handlers executed during event propagation?

## Event Listeners

12. How does `addEventListener()` work?
13. What are the differences between:
    - `capture`
    - `once`
    - `passive`
14. How do you properly remove an event listener?
15. What are common memory leak issues related to event listeners?

## Modern Browser APIs

16. What is the Intersection Observer API and when would you use it?
17. What is the Mutation Observer API and when would you use it?
18. What is the Resize Observer API and when would you use it?

## Browser Lifecycle & Performance

19. What is the difference between `DOMContentLoaded` and `load` events?
20. What are common DOM performance bottlenecks and how can they be avoided?
