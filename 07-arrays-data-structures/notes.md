# 07 Arrays & Data Structures

## Arrays Fundamentals

1. How are Arrays stored in memory in JavaScript?
2. What is the time complexity of common Array operations?
3. Why are `push()` and `pop()` generally faster than `shift()` and `unshift()`?
4. What is the difference between mutable and immutable Array methods?

## Array Methods

5. What is the difference between `slice()` and `splice()`?
6. What is the difference between `map()`, `forEach()`, and `filter()`?
7. When would you use `map()` instead of `forEach()`?
8. What is the difference between `find()`, `findIndex()`, `some()`, and `every()`?
9. What is `reduce()` and what are its most common use cases?
10. Can you implement common Array operations using `reduce()`?
11. How would you remove duplicate values from an Array?
12. What are different ways to flatten a nested Array?
13. How would you sort numbers correctly in JavaScript?

## Data Structures

14. What is a Set and when would you use it?
15. What is the difference between Set and Array?
16. What is a Map and when would you use it?
17. What is the difference between Map and Object?
18. What are WeakMap and WeakSet, and when would you use them?
19. When would you choose Array, Object, Map, or Set in a real application?

## Advanced Concepts

20. What are Array-like Objects and how do you convert them into Arrays?
21. What are common performance pitfalls when working with large Arrays?
22. What are common real-world use cases of Map and Set in frontend applications?
