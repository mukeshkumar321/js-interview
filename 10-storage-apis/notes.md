# 10 Storage APIs

## Storage Fundamentals

1. What are the different client-side storage options available in browsers?
2. What is `localStorage` and when would you use it?
3. What is `sessionStorage` and when would you use it?
4. What is the difference between `localStorage` and `sessionStorage`?
5. What are Cookies and why are they used?
6. What is the difference between Cookies and Web Storage (`localStorage` / `sessionStorage`)?

## Security Considerations

7. What data should never be stored in `localStorage`?
8. Why is storing JWT tokens in `localStorage` considered risky?
9. What is XSS and how does it impact browser storage?
10. What are HttpOnly, Secure, and SameSite cookies, and why are they important?

## IndexedDB

11. What is IndexedDB and when would you use it?
12. What is the difference between IndexedDB and `localStorage`?
13. When would you choose IndexedDB over `localStorage`?

## Real-World Usage

14. How do you handle data expiration in `localStorage`?
15. How can you synchronize `localStorage` changes across multiple browser tabs?
16. What is the `storage` event and when is it triggered?
17. How would you persist application state across page refreshes?
18. How would you choose between Cookies, `localStorage`, `sessionStorage`, and IndexedDB?
