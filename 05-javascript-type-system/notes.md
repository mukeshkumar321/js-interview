# 05 JavaScript Type System

## Primitive vs Reference Types

1. What are the different data types in JavaScript?
2. What is the difference between Primitive and Reference Types?
3. How are Primitive Values stored in memory?
4. How are Objects and Arrays stored in memory?
5. What is the difference between Pass by Value and Pass by Reference?
6. What happens when you assign one object to another variable?
7. Why are Objects compared by reference instead of by value?

## Type Coercion

8. What is Type Coercion? Explain Implicit vs Explicit Coercion.
9. What are Truthy and Falsy values in JavaScript?
10. What is the difference between `==` and `===`?
11. What is the difference between `null` and `undefined`?
12. Why does `typeof null` return `"object"`?
13. What is `NaN` and why is `NaN !== NaN`?
14. What is the difference between `isNaN()` and `Number.isNaN()`?
15. What is `Object.is()` and how is it different from `==` and `===`?

## Type Checking & Modern Features

16. How does the `typeof` operator work and what are its limitations?
17. What is the difference between `typeof`, `instanceof`, and `Array.isArray()`?
18. What are Optional Chaining (`?.`) and Nullish Coalescing (`??`)?
19. What is the difference between `||` and `??`?
20. What are common bugs caused by automatic type coercion?
